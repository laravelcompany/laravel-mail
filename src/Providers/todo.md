# TODO

- Merge the Providers into a single file.
- Add the ability to use the MailServiceProvider as a stand-alone application.
- Add the ability to use the MailServiceProvider as a package inside an existing application.
